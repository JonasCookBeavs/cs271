DISCLAIMER: The following is only true if the code worked, which it doesn't

To run the program, simply run "./main" followed by the directory of the .asm file you wish to run
The program will then create a new ".hack" file with the instructions converted to binary.
Works with address, label, and c-type instructions.
